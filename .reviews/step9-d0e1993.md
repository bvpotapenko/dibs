# dibs — Checkpoint 2 review (ARCHITECTURE §13 steps 5–9)

Reviewed HEAD: d0e1993773cb2a18dbf860568317b88950126793
(`d0e1993 feat(core): implement names, output envelope, views bodies (ARCHITECTURE §13 steps 8-9)`)
Working tree clean at review time. Governing docs at HEAD: SSoT **Rev 10**
(the brief says Rev 8; per SSoT precedence "the current Rev governs"),
ARCHITECTURE §3 budgets as written at HEAD (queries 7, output 6, cli 6,
17 files — the brief's Rev 8 numbers are superseded).

Tooling: `make lint`/`make test` must run with the repo venv
(`PY=.venv/bin/python make test`, or `export PATH=.venv/bin:$PATH`); the
system python3 has no pytest. flake8 7.3.0 + wemake-python-styleguide 1.8.0
confirmed loaded (`flake8 --version`), ruff 0.16.5, SQLite 3.53.3.

## Rework order preamble

You are Claude Opus 5 executing a rework order for dibs at commit
d0e1993. Read CLAUDE.md, docs/SSoT.md, docs/ARCHITECTURE.md;
SSoT wins. Rules: fix ONLY the findings below, smallest change that
satisfies the cited contract; §3 member budgets are ceilings; do NOT
add noqa or touch lint config; tests may be added or strengthened,
never weakened or deleted; one commit per finding ID; after each fix
run `make lint && make test` and confirm the finding's REPRO now
passes; if a finding seems wrong, STOP and report disagreement
instead of fixing around it. Finish by re-running the full suite and
reporting per-finding status.

## VERDICT: BLOCKED

The checkpoint is posed for a tree that does not exist yet: ARCHITECTURE §13
steps 10 (`verbs/`) and 11 (`cli`, `trace`) have not landed, so the pipeline,
exit codes, D21/D18/D20/D23 and the pyz life cycle cannot be exercised; 16
tests still raise `NotImplementedError` by design. Everything that did land
(steps 5–9: queries, plansync, names, output, views, Rev 9/10 amendments) was
reviewed adversarially by hand-driving the §6 pipeline from the lower modules;
lint is silent and the 94 reachable tests are green, but four contract defects
on the read/render side (F2–F5) must be fixed before step 10 builds on them.

## Findings

[F1] BLOCKER
WHERE: dibs/cli.py:48,61,66,75,80,92; dibs/trace.py:38,43; dibs/verbs/work.py:18,28,38,43; dibs/verbs/board.py:20,31,41,46,56; tests/test_cli.py (10 cases); tests/test_trace.py (6 cases)
CONTRACT: ARCHITECTURE §13 "10. verbs/ … 11. cli.py, __main__.py, trace.py … 12. zipapp build; smoke-test"; §11 "Definition of done per module: its tests green"; checkpoint brief "ALL 88+ tests must now be green — any surviving NotImplementedError or skip is a BLOCKER".
EVIDENCE: `PY=.venv/bin/python make test` → `16 failed, 94 passed` — every failure `NotImplementedError: needs cli.main (§13 step 11)` / `needs trace.trace_path (§13 step 11)`. `grep -rn NotImplementedError dibs` → 17 stubs, all naming step 10 or 11. `make build` produces dist/dibs.pyz (73025 bytes); `python dibs.pyz verify plan.md` in a temp dir → `NotImplementedError: ARCHITECTURE §13 step 11: cli.main`, exit 1; `python -m dibs list` → same. The brief's list "steps 5-9 … verbs/, cli, trace" does not match §13's numbering; the tree matches "steps 1–9 done, next step 10".
REPRO: `PY=.venv/bin/python make test | tail -1` (must read `110 passed`); `PY=.venv/bin/python make build && cd $(mktemp -d) && cp /Users/bd/projects/dibs/dist/dibs.pyz . && printf -- '- [ ] Fix it\n  body\n' > plan.md && python3 dibs.pyz verify plan.md; echo exit=$?` (must print the preview and `exit=0`).
FIX DIRECTIVE: Not a rework item — land §13 steps 10 and 11 per ARCHITECTURE §5/§6 (after F2–F5 below, which change what the verbs must call), then re-run this checkpoint in full: attack-list items 1–3 and 6–9 (pipeline order, D21 byte-compare, D18/D20 resolution, exit codes 0/1/2, I4 end-to-end, D23 trace) remain unverified and are NOT covered by this review. Budget impact: none now; see NOTES for the step-10/11 hazards found while reading.

[F2] SHOULD-FIX
WHERE: dibs/output.py:73-76 (`Refusal.UNKNOWN_TASK` command `'dibs {2} {1}'`); pinned wrong at tests/test_queries.py:112
CONTRACT: SSoT §6 `done <ID> --note "…"` — "`--note` is mandatory"; C7/I10 "Every steer is a runnable command"; D14 "every error … contains the literal next command".
EVIDENCE: probe P2 — `queries.resolve_task(conn, 'A9', 'done')` → rendered error `Unknown task A9 - did you mean A3?\nRun: dibs done A3`. `dibs done A3` cannot succeed as printed (no `--note`); the same catalog line is correct for `claim`/`drop` only. test_steer_every_refusal_is_runnable never sees it because SAMPLE_NAMES uses verb `claim`.
REPRO: `.venv/bin/python -c "from dibs.output import *; assert steer(Refusal.UNKNOWN_TASK, ('A9','A3','done')).steer == 'dibs done A3 --note \"...\"'"`
FIX DIRECTIVE: Make the UNKNOWN_TASK steer runnable for every caller verb, text staying in output (C5) and the `(message, command)` catalog shape and `steer` factory intact (C7): e.g. a `VERB_FORMS` MappingProxyType constant in output — `claim` → `dibs claim --task {1}`, `done` → `dibs done {1} --note "..."`, `drop` → `dibs drop {1}` — selected by the verb slot when the UNKNOWN_TASK command is formatted. Strengthen tests/test_queries.py::test_resolve_task_miss_steers to expect `dibs done A3 --note "..."` and extend test_output.py::test_steer_unknown_task_is_the_callers_verb to all three verbs. No member added; constants don't count.

[F3] SHOULD-FIX
WHERE: dibs/queries.py:93-120 (`REFUSAL_SQL` CASE has no branch for a bundle member absent from `tasks`); dibs/transitions.py:47 and dibs/queries.py:97,102,128,137 (`json_each` members compared case-sensitively)
CONTRACT: D14 "Unknown task B7 — did you mean A7? Run: `dibs claim --task A7`" (the canonical example is a *claim*); SSoT §6 "fuzzy ID matching … tolerant argument forms"; C9 "if a read needs to pick between outcomes, it returns a Refusal from a CASE"; I10; ARCHITECTURE §5 claim_refusal "why claim returned ()".
EVIDENCE: probe P1 on the fixture board — `transitions.claim(conn, otter, NOW, ('Z9',))` → `()`; `queries.claim_refusal(conn, otter, ('Z9',))` → `(Refusal.WAITING, ('5', None, None))`, rendered `Nothing available yet: 5 task(s) wait on None (held by None) - retry after finishing something else, or stop if your launcher respawns workers.\nRun: dibs claim`. P1b: `('A1', 'Z9')` with max_hand 3 → the same WAITING/None text although A1 is free. P1c: `claim(('a1',))` → `()` and the same misdiagnosis, while `resolve_task(conn, 'a1', 'done')` resolves to A1 — `done` tolerates case, `claim --task` does not. ARCHITECTURE §9's claim row lists no resolve_task, so nothing upstream canonicalizes members.
REPRO:
    .venv/bin/python -c "
    import tempfile, pathlib
    from dibs import queries, transitions
    from dibs.output import Refusal
    from tests.conftest import PLAN_TEXT
    from tests.boards import NOW, OTTER, build_board
    ctx = build_board(pathlib.Path(tempfile.mkdtemp()), PLAN_TEXT)
    assert transitions.register_agent(ctx.conn, OTTER, NOW)
    assert not transitions.claim(ctx.conn, OTTER.agent_id, NOW, ('Z9',))
    kind, names = queries.claim_refusal(ctx.conn, OTTER.agent_id, ('Z9',))
    assert kind is Refusal.UNKNOWN_TASK and names[0] == 'Z9' and None not in names, (kind, names)
    assert [t.task_id for t in transitions.claim(ctx.conn, OTTER.agent_id, NOW, ('a1',))] == ['A1']"
FIX DIRECTIVE: (a) In `claim_refusal`, add a first CASE branch — a `json_each(:bundle)` value not present in `tasks.id` → `'unknown_task'` (`Refusal.UNKNOWN_TASK` already exists) — and a names query returning `(that member, its nearest id by the NEAREST_SQL ordering, 'claim')` so the tuple feeds `output.steer` like every other kind; bind `letters`/`tail` alongside the existing named bindings. (b) D14 case tolerance: normalize bundle members (`upper()`) in `transitions.claim` and `claim_refusal` before they reach `json_each`, matching `EXACT_SQL`'s `UPPER(?1)`. Add a test_queries case (unknown member alone, unknown beside a free member, lowercase member claims) and assert the rendered steer starts with `dibs claim`. Budget: no new member. Note for the architect: ARCHITECTURE §9 claim row should say whether members are canonicalized in SQL (this fix) or via resolve_task in the verb.

[F4] SHOULD-FIX — architect decision required before code moves
WHERE: docs/ARCHITECTURE.md §6 step 4 (`if plan.stat().st_mtime_ns != board.plan_mtime: apply_sync`) and step 8 (`tempfile + os.replace`, no mtime restamp); dibs/plansync.py:103-107 (SYNC event written unconditionally; `MTIME_SQL` reachable only through `apply_sync`)
CONTRACT: D14 "Output is terse by contract … events one line each … in an 8k window every dibs token competes with code"; I9 (mtime check exists to detect *human* edits); I6/C3 "Every mutation writes exactly one event" / "A sync is one mutation with one SYNC event" — an empty diff mutates nothing; §6 step 4 "auto-sync (I9), silent".
EVIDENCE: probe P5, driving §6 literally with the landed modules: after `claim` the settle tail's annotation write (tempfile + os.replace) leaves `meta.plan_mtime != plan.stat().st_mtime_ns` (printed `False`); the next invocation's step-4 check fires `apply_sync` on the annotated text → facts `() () () ()`, one event appended; `deliver_events` for the other agent then renders `sync: synced 6 tasks: nothing changed` beside the real `claim A1 by brave-otter: …` line. Every claim/done/drop/reap rewrites plan.md, so every agent's next command will carry one such line per board mutation, and each one consumes a slot of the 15-event cap.
REPRO: `.venv/bin/python /private/tmp/claude-501/-Users-bd-projects-dibs/059cbf1e-0cd9-4bd9-a3c5-190e75af2f53/scratchpad/probe.py 2>&1 | sed -n '/=== P5/,/second annotation/p'` — must end with elephant's feed showing no `sync: … nothing changed` line.
FIX DIRECTIVE: Opus: do NOT implement; report "awaiting architect". Options, smallest first: (E, recommended) an empty diff journals nothing — make the SYNC INSERT conditional in SQL (`INSERT … SELECT … WHERE ?` bound to "any of new/vanished/checked/regressed non-empty"; branching in the statement per C9), amend C3/§8 wording ("a sync that changes nothing journals nothing"), and change tests/test_plansync.py::test_apply_sync_same_text_only_journals to assert zero new events (a changed pinned decision, not a weakening — architect's call); optionally have §6 step 8 re-run `apply_sync` on the text it just wrote so `plan_mtime` never drifts. (A) restamp `plan_mtime` after the annotation write — needs a SQL home; `plansync` (2/2) and `store` (4/4) are at budget, so a §3 amendment first. Rejected: `os.utime` restore (fights the filesystem), content hashing (SSoT §5 says mtime).

[F5] SHOULD-FIX
WHERE: dibs/views.py:30,127 (`YOU_ARE = 'you are {0}'` rendered with the *name*); dibs/output.py:38 (`HINTS['claim']` carries no identity); pinned at tests/test_views.py:79,87
CONTRACT: SSoT §6 claim row "Mint identity on first use (fallback when `join` wasn't used) … Returns **id reminder**, task title + body, events, next verb"; D8 "IDs are for command input; names are for display … `claim`-time minting remains as fallback"; I7 "Session ids appear only in the owning agent's own command I/O" (claim output *is* that I/O); I10 "output alone enough for an agent with no other context to take its next correct step"; §11 "Lost your id → … mint a fresh identity with your next `claim`".
EVIDENCE: probe P7 — the briefing opens `you are happy-elephant` and ends `next: dibs done A1 --note "..."`. A worker minted at claim time (no `DIBS_AS`) has no id anywhere in its output; the hinted `done` then runs with actor None → `finish` WHERE owner=NULL → 0 rows → NOT_OWNER → its steer `dibs claim --task A1` mints yet another identity → TAKEN → `dibs claim` claims a fresh task under a third ghost. The documented fallback path cannot complete a task; ARCHITECTURE §5/§6 say "you are <name>", SSoT says "id reminder" — SSoT wins.
REPRO: `.venv/bin/python -c "from dibs.views import format_briefing; from tests.boards import ELEPHANT; out='\n'.join(format_briefing((), ELEPHANT.agent_id, ())); assert ELEPHANT.agent_id in out, out"` (after the fix the verb passes the id; the render must show it).
FIX DIRECTIVE: Render the identity reminder with the id, not the bare name (the id contains the name, so display hygiene holds and I7 permits it in the owner's own I/O); recommended terse form for the minted-this-invocation case: `you are happy-elephant-4821 - export DIBS_AS=happy-elephant-4821` (one line, D8's preferred binding as a runnable command). Keep the signature at ≤4 params (`format_briefing(tasks, actor, priors)` with `actor` the id; if the export line is conditional on minting, a fourth bool param is within budget). Strengthen test_views.py::test_briefing_identity_title_body_priors to assert the id (and the export line) appears. Flag for the architect: ARCHITECTURE §5 (`actor_name`) and §6 ("you are <name>") need a one-line amendment; step 10's `claim_task` must pass `ctx.actor or minted.agent_id`.

[F6] CONTRACT-POLISH
WHERE: dibs/views.py:12 (`from dibs.planfile import SyncPlan`)
CONTRACT: ARCHITECTURE §4 "A module imports only **strictly lower** levels (plus stdlib)"; §4 table: `views` L1, `planfile` L1; precedent dibs/runtime.py:13 (`if TYPE_CHECKING:` for a same-level type edge, "a type-only edge … not a runtime dependency").
EVIDENCE: `grep -n planfile dibs/views.py` → line 12, a runtime import used only in the `format_sync(plan: SyncPlan)` annotation. Every other edge in the package respects §4 (checked by eye: output→records/runtime, queries→output/records, names→transitions/records, plansync→planfile/queries/views/records, cli→runtime/verbs).
REPRO: `.venv/bin/python -c "import ast,pathlib; t=ast.parse(pathlib.Path('dibs/views.py').read_text()); assert not any(isinstance(n, ast.ImportFrom) and n.module=='dibs.planfile' for n in t.body), 'runtime L1->L1 import'"`
FIX DIRECTIVE: Guard the import under `TYPE_CHECKING` and quote the annotation (`plan: 'SyncPlan'`), exactly as runtime.py does for `Event`. No budget impact.

[F7] CONTRACT-POLISH
WHERE: dibs/output.py:78-81 (`Refusal.NOT_OWNER` message `'{0} is not yours - {1} holds it; you were probably reaped.'`)
CONTRACT: D14 "Every error steers rather than diagnoses"; ARCHITECTURE §6 "steer(NOT_OWNER, (task_id, holder_name)) — the holder comes from the resolve_task row"; SSoT §11 "`done` rejected → you were probably reaped" (the most common case, in which the row is `todo` and unowned).
EVIDENCE: probe P3 — after a reap `resolve_task(conn,'A1','done').owner` is None, `agent_name(None)` is `''`, and the prescribed tuple renders `A1 is not yours -  holds it; you were probably reaped.` (double space, no holder). The TAKEN names query already solves the same problem in SQL with `COALESCE(agents.name, tasks.owner, tasks.status)`.
REPRO: `.venv/bin/python -c "from dibs.output import *; m=steer(Refusal.NOT_OWNER, ('A1','')).message; assert '  holds' not in m and 'todo' in m, m"`
FIX DIRECTIVE: Make the message correct for an unowned row — either word slot 1 as the task's current state ("{0} is not yours (now {1}) — you were probably reaped") with the caller passing the holder name or, when unowned, the status, or have the step-10 verb pass `agent_name(owner) or status.value`. Add a test_output case for the unowned tuple. No budget impact.

[F8] CONTRACT-POLISH
WHERE: dibs/output.py:128-132 (`Refusal.MANY_BOARDS`)
CONTRACT: D18 "Several found → **refuse and enumerate**, each option with a runnable steer ("Run: `dibs claim --plan errands.md`")"; skeleton tests/test_cli.py::test_many_boards_enumerated_refusal docstring "one runnable steer per board"; I10.
EVIDENCE: probe P4 — `steer(Refusal.MANY_BOARDS, ('claim', 'errands.md, refactor.md'))` renders `Several boards in scope: errands.md, refactor.md. Pick the one matching the plan path you were given - never guess.\nRun: dibs claim --plan <one of: errands.md, refactor.md>`. No option is printed in runnable form; the only `Run:` line is a placeholder. Step 11's resolve_board will have no C5-compliant way to satisfy the pinned test with this entry.
REPRO: `.venv/bin/python -c "from dibs.output import *; e=steer(Refusal.MANY_BOARDS, ('claim','errands.md, refactor.md')); assert 'dibs claim --plan errands.md' in render_error(e), render_error(e)"`
FIX DIRECTIVE: Render each board as its own literal `dibs <verb> --plan <path>` line inside the message (the terminal `Run:` line may stay the choice form, since no single command is right); the join must happen in output (C5) — e.g. the caller passes the paths joined with a documented output separator constant that itself carries the per-option prefix, or `steer` maps an `OPTION` template over `names[1:]` for this kind. Keep one factory, no new member; add the rendered per-board lines to SAMPLE_NAMES-based assertions in test_output.py.

[F9] CONTRACT-POLISH — docs (architect)
WHERE: docs/ARCHITECTURE.md §3 table `records.py … [6]` vs dibs/records.py (7 members: Status, EventKind, Task, Event, Agent, Board, agent_name; module docstring says "Member budget 7")
CONTRACT: ARCHITECTURE §3 "Budgets are the plan, not a hope"; §3 prose "used at exactly two render sites … a third site would earn `records.agent_name`".
EVIDENCE: AST count → records members = 7; `grep -rn agent_name dibs` → planfile.py:321, output.py:175-176, views.py:87 — the third site exists, so the member is pre-sanctioned, but the table and the two-site sentence were not amended in d0e1993 (which touched §5, C11, §11 only). Every other module is at or under its table budget (runtime 3/3, store 4/4, planfile 7/7, output 6/6, views 3/3, trace 3/3, transitions 6/6, queries 7/7, plansync 2/2, names 2/2, cli 6/6+VERB_TABLE, work 4/4, board 5/5; 17 files vs "17 … ≤18").
REPRO: `grep -n "records.py" docs/ARCHITECTURE.md` shows `[6]`.
FIX DIRECTIVE: Architect: amend §3 to `[7]` naming `agent_name` and replace the "two render sites" sentence with the three sites. Opus: do not edit docs; report as awaiting architect. No code change.

[F10] CONTRACT-POLISH
WHERE: tests/boards.py:67-81 (`set_max_hand`, `orphan` — raw `UPDATE`s); tests/test_queries.py:149 (`UPDATE tasks SET seq = 10 - seq`, comment "as apply_sync will do it (§13 step 7)")
CONTRACT: CLAUDE.md "Test boards are seeded through production code … Raw SQL in tests is for assertion peeks only"; ARCHITECTURE §11 "From step 7 on it calls plansync.found_board + apply_sync … Peeks … stay: they are assertion reads, not production paths".
EVIDENCE: `grep -n -E "execute(many)?\(.*(UPDATE|INSERT|DELETE)" tests/*.py` → the three writes above (plus test_store.py:34, which exercises store itself). Production paths now exist for all three: `plansync.found_board(conn, now, key, max_hand)` stamps max_hand; re-applying the plan with lines removed orphans; re-applying reordered text renumbers seq (the step-7 comment is stale).
REPRO: the grep above must list only test_store.py.
FIX DIRECTIVE: Route `set_max_hand` through `plansync.found_board` (adjust test_queries.py::test_board_snapshot_carries_meta_and_events to expect the founded key), replace `orphan` with a resync of the fixture text minus the lines, and reorder in test_board_snapshot_in_seq_order by resyncing reordered text (tests/test_plansync.py::resync is the pattern). Never weaken an assertion; expected values stay.

## NOTES (non-findings)

- **Scope actually verified.** Attack-list items 4 (I7 surfaces), 5 (D10), 9 (D22/D7, prior-claim, resolve_task), 10 (C-sweep), 11 (line count), plus the D21/D24 verify-equals-init property, were verified on the landed modules; items 1–3 and 6–8 need cli/trace and are open (F1).
- **Tests vs skeleton:** no case deleted, skipped, or hollowed (AST sweep: every test has asserts or a `pytest.raises`; `only()` asserts inside). Moves accounted for: test_output's two `format_preview` cases → test_views (`test_board_shows_tree_progress_and_key`, `test_board_warns_bodiless_and_dupes`); test_transitions `test_import_author_done_owner_human` → test_plansync `test_apply_sync_imports_hand_checked_as_human`. Docstrings only strengthened; checkpoint-1 F5's relaxed idempotence is now the strict one-pass assertion (test_property_planfile:181-195). Config diff since the skeleton: only the S311 test ignore ARCHITECTURE §10 sanctions. No `noqa` in dibs/ or tests/. Docs edits are the architect's (SSoT r9/r10, ARCH §5/C11/§11/§13).
- **Verified D10:** cursor advances in the delivering transaction (second call empty), own events never echoed, directed notes reach only their target, `None` actor gets nothing; 18 unseen events render as 15 lines + `... and 3 more - run: dibs list` (probe P9 rendered `... and 12 more` over a longer feed). Observation: the feed shows the *oldest* 15 and hides the newest; `dibs list` shows the newest 15 — if the overflow exceeds 15, the middle is on neither surface (cursor already advanced). Within SSoT §13's "~15"; noted for the architect.
- **Verified I7:** no `-NNNN` id on format_board, format_briefing, format_event output, or the annotated plan.md (probe P6); reap events carry the former holder's *name*; TAKEN/WAITING names come from `agents.name`.
- **Verified D22/D7:** `newly_unlocked` None after A2.1, the A2 row after A2.2, hint `dibs claim --task A2`; property gating invariant green. `claim_refusal` kinds all six correct on the fixture; WAITING names the caller itself when it holds the gate (probe P11: "wait on A1.1 (held by brave-otter)" to brave-otter — actionable).
- **GATED steer chain (probe P10):** when every open child is held by others, the steer `dibs claim --task A1.1` runs into TAKEN ("A1.1 is already taken (brave-otter)") which steers to `dibs claim`. Two round trips, both runnable — not a finding; a `dibs claim` steer when no child is todo would save one.
- **Verified D24/D21 at module level:** `format_board(compute_sync(items, ()).rows, '')` equals the live board render minus the key header (test + probe); `verify`'s "waits-for" reading is the indented tree plus `(done/all)` progress, per ARCHITECTURE §5.
- **C-sweep:** C1 — only `__main__.py:8 sys.exit(cli.main())` (the §3 entry, sanctioned) and a string literal inside the OLD_SQLITE steer; C2 — no SQL outside store/transitions/plansync/queries; C6 — no regex outside planfile; C8 — no mutable module state (AST sweep); C5 — the only f-strings outside output/views build ids/keys (names, planfile.mint_id, queries.ID_TAIL) and planfile's own annotation grammar.
- **Hazards for steps 10/11 (design gaps the implementer will hit; architect should pre-decide):**
  1. `join` with `DIBS_AS` already set: the settle tail's `deliver_events(actor)` would append a feed after the bare id, breaking `export DIBS_AS=$(dibs join)` — join's Reply must render with no feed/hint (deliver for actor None, or skip step 7 for join).
  2. Three user-facing lines have no template home yet, while views (3/3) and output (6/6) are at budget and verbs may not format (C6): verify's "board already exists → one line → list" (D21), note's "unknown name — still logged" warning (SSoT §6), and `list`'s recent-events body (step 7 overwrites `Reply.events` with the piggyback feed, so `Board.events` must become lines via `format_event`).
  3. `--task`/`--as` argparse errors: argparse exits 2 with its own text unless `build_parser` funnels them into a DibsError (D14/I10, exit 1).
  4. `next_hint('empty', …)` is unreachable (EMPTY is a refusal); harmless constant.
- **Pre-existing design notes, not findings:** the §6 step-8 read→annotate→replace window can clobber a human edit made inside it (I9 says any time; the window is milliseconds). A note `--for` given an *id* instead of a name is stored verbatim and happens to deliver, while the verb's unknown-name warning would fire. Mixed tab/space nesting and CR-only files as in checkpoint 1.
- **Budget report (item 11):** 1267 code lines (non-blank, non-comment, non-docstring) / 2007 physical in 17 files at HEAD; of those ~75 code lines are step-10/11 stub scaffolding. SSoT §2 (Rev 9+) budgets *code* lines: target ~1000, hard stop 1500 (the brief's "~600 / 1000" is the Rev 8 physical-line figure). Projecting verbs (~120), cli (~130) and trace (~30) puts the finished package at roughly 1450–1550 — at or past the hard stop. Recommendation: an SSoT §2 Rev before step 11 lands (restate the budget by measurement, as r9 did, or name what to re-scope), rather than discovering it at step 12.
- **Bundle-event reading observed:** unchanged from checkpoint 1 — one event per changed row (N claim events for a bundle of N, one reap per reaped task, one DONE per hand-checked import inside a sync, one SYNC per sync).
- Copy saved as `.reviews/step9-d0e1993.md` (the brief's `step4-` name belongs to checkpoint 1; `.reviews/step4-96e23df.md` is tracked, this file is left untracked).
